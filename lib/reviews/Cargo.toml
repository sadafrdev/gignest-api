[package]
name = "reviews"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.6", features = ["runtime-tokio-rustls", "postgres"] }
serde = { version = "1.0", features = ["derive"] }
utils = { path = "../utils" }
